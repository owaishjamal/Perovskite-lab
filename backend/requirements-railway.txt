# Railway-optimized requirements with CPU-only PyTorch
# Install with: pip install -r requirements-railway.txt

# Core dependencies
fastapi==0.123.4
uvicorn[standard]==0.38.0
pandas==2.3.3
numpy==2.3.5
joblib==1.5.2
pydantic==2.12.5
scikit-learn==1.7.2
python-multipart==0.0.9

# PyTorch CPU-only (install separately to avoid build issues)
# Run: pip install torch==2.9.1+cpu --index-url https://download.pytorch.org/whl/cpu

